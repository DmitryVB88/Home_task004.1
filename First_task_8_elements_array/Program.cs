// Задать массив из 8 элементов.
int arrSize = int.Parse(Console.WriteLine());
int[] array = [arrSize];
for(int i = 0; i < array.Length; i++) 
array[i] = new Random().Next(0,2);
for(int i = 0; i < array.Length; i++)
{
    Console.Write($"{array[i] + " "}");

}

p = array[I]*array[arry.Length-i-1] - решение перемножение пар. 

